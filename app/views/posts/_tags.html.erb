<div class="d-flex mb-3 flex-wrap">
  <% post.tags.each do |tag| %>
    <%= link_to tag.label, posts_path(tag: tag.label), class: "badge mt-1 text-white me-1 #{"fs-sm" unless defined?(small) && small}", style: "background-color: #{tag.color}" %>
  <% end %>
</div>